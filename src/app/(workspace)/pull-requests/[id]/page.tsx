// OWNER: Person B — PR Risk Detail drill-down (see UI spec "PR Risk Detail").
// Placeholder so the route is a valid module; build out the real screen here.
export default function PullRequestDetailPage() {
  return <div>PR Risk Detail — coming soon</div>;
}
