// OWNER: Person C — Repository Detail drill-down (see UI spec "Repository Detail").
// Placeholder so the route is a valid module; build out the real screen here.
export default function RepositoryDetailPage() {
  return <div>Repository Detail — coming soon</div>;
}
